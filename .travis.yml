language: bash
sudo: false
install: echo sure
script:
- curl -sSL http://download.nextag.com/apache//ant/binaries/apache-ant-1.9.4-bin.tar.bz2 | tar --strip-components=1 xjf -
- ./bin/ant -version
- ./bin/ant
